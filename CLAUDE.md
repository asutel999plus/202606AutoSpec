# プロジェクト運用ルール（AutoSpec）

## バージョン管理（git/svn）の状態を変更しない

Claude Codeがこのプロジェクトで作業する際、git/svnの状態を変更する操作は、
ユーザーから明示的に指示されない限り行わない。

### 禁止される操作（恒久ルール）
- `git add` / `git commit` / `git push`
- `git mv`、ブランチの作成・削除・切替、リモート設定の変更など、
  バージョン管理の状態を変更するすべての操作
- ファイルのリネーム・移動を「git管理下だから」という理由だけで
  自動的に `git mv` 等のgit側操作に反映させること

### 許可される操作
- `git status` / `git log` / `git diff` など、閲覧・差分確認のみを行う
  読み取り専用コマンドは実行してよい

### ファイルのリネーム・移動の方法
- ファイルのリネーム・移動は、OSのファイル操作（`rename` / `move` /
  `Rename-Item` / `Move-Item` 等）で行う
- git管理下にあるファイルであっても、リネーム・移動時に
  `git add` や `git mv` 等のgit側の操作を自動的に追加で行わない
  （gitへの反映が必要かどうかはユーザーが判断する）

**Why:** AutoSpecツールの開発・検証作業において、Claude Codeが
意図せずgit/svnの管理状態を変更してしまうと、作業の追跡や差分確認が
困難になり、意図しない変更が混入するリスクがあるため。

**How to apply:** 本プロジェクト内での全作業（Setup.bat/AutoSpec.bat
関連の実装作業を含む）に適用する。ファイル操作を行う際は、まず
git管理下のファイルへの影響がないか確認し、git状態を変更する操作が
必要だと判断した場合は、実行前に必ずユーザーに確認を取ること。
